lists=[]
for i in range(9):
    n=int(input())
    lists.append(n)
print(max(lists)) # input 9개를 list에 넣어 그 중 가장 큰 수 구하기
print(lists.index(max(lists))+1) # 그 가장 큰 수가 list에서 몇 번째인지 구하기. 단, index는 0부터 세므로 1을 더한다.
